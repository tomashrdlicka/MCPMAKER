{
  "name": "@mcpmaker/extension",
  "version": "0.1.0",
  "private": true,
  "description": "MCPMAKER Chrome Extension - Record and replay web workflows",
  "scripts": {
    "build": "tsc && node scripts/copy-public.mjs",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "prebuild": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.0"
  }
}
